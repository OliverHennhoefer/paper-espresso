[project]
name = "paper-espresso"
version = "0.3.0"
description = "Create compact, technically faithful references for research papers."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "Paper Espresso contributors" }]
dependencies = []

[project.optional-dependencies]
docs = ["zensical==0.0.51"]

[tool.uv]
package = false
